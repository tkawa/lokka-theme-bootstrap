<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
<% if entry? %>
    <title><%= @entry.title %> - <%= @site.title %></title>
<% else %>
    <title><%= @site.title %></title>
<% end %>
    <meta name="description" content="<%= @site.meta_description %>" />
    <meta name="keywords" content="<%= @site.meta_keywords %>" />
    <link rel="alternate" href="/index.atom" type="application/atom+xml" />
    <link rel="stylesheet" href="<%= @theme.path %>/stylesheets/style.css" />
    <%= header %>
  </head>
  <body>

    <div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="/"><%= @site.title %></a>
          <ul class="nav">
            <li><a href="/">Home</a></li>
<% Page.all.each do |page| %>
            <li><a href="<%= page.link %>"><%= page.title %></a></li>
<% end %>
          </ul>
         <form class="pull-right" method="get" action="/search/">
            <input type="text" name="query" placeholder="Search" />
            <button class="btn" type="submit">Search</button>
          </form>
        </div>
      </div>
    </div>

    <div class="container">

      <div class="content">
        <div class="row">
          <div class="span16">
            <%= yield %>
          </div>
        </div>
      </div>

      <footer>
        <p class="copy"><a href="/"><%= @site.title %></a></p>
        <p class="poweredby wide">powered by <a href="http://lokka.org/">Lokka</a></p>
      </footer>

    </div>

  </body>
</html>
